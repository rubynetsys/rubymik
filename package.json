{
  "name": "rubymik",
  "version": "1.1.5",
  "private": true,
  "description": "Modern, self-hosted monitoring dashboard for MikroTik RouterOS devices",
  "license": "MIT",
  "author": "Ray Roberts / Rubynet (Pty) Ltd",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build && npm --prefix server run build",
    "start": "npm --prefix server run start"
  }
}
